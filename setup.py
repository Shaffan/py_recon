from setuptools import setup

setup(name='py_recon',
      version='0.12',
      description='Website recon tool',
      author='Stefan Horne',
      license='MIT',
      packages=['py_recon'],
      zip_safe=False)